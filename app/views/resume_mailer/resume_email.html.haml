!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv"=>"Content-Type"}
  %body
    %h1 #{@user.name}
    %p=link_to @user.login, @user.html_url
    %p #{@user.email}
    %p=link_to "Website", @user.blog
    %table
      %tr
        %th Repo Name
        %th Primary Language
        %th Created at
        %th Last Updated
        %th Stars
        %th Forks
      -@repos.each do |repo|
        %tr
          %td=link_to repo.name, repo.html_url
          %td=repo.language
          %td Created #{time_ago_in_words(repo.created_at)}
          %td Last updated #{time_ago_in_words(repo.updated_at)}
          %td=repo.watchers_count
          %td=repo.forks_count
